package:
  name: r-tidyverse
  version: 1.2.1

build:
  number: 1
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  host:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('fortran') }}
    - r-base
  run:
    - r-base

about:
  home: https://github.com/tidyverse/tidyverse
  summary: R packages for data science
  license: Various
