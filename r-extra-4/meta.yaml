package:
  name: r-extra-4
  version: 3.4.3

build:
  number: 0
  rpaths:
    - lib/R/lib/
    - lib/

requirements:
  host:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ compiler('fortran') }}
    - r-base
    - r-essentials
    - r-machinelearning
  run:
    - r-base
    - r-essentials
    - r-machinelearning

about:
  summary: Additional R packages that will be merged into other views in a future release
  license: Various
